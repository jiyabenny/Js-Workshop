let check=function(n){
    if((n%2)==0){
        console.log(`The number entered is ${n} and it  is even`);
    }
    else{
        console.log(`The number entered is ${n} and it is odd`);
    }
}
let num =Number(prompt("enter the number"));
check(num); 

